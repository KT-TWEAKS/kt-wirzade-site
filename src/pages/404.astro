---
---
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Página não encontrada · KT WIRZADE</title>
  <meta name="robots" content="noindex">
  <meta name="theme-color" content="#030303">
  <link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 32 32'%3E%3Crect width='32' height='32' rx='7' fill='%23ef4444'/%3E%3Ctext x='50%25' y='56%25' text-anchor='middle' dominant-baseline='middle' font-size='14' font-weight='bold' fill='%23ffffff' font-family='monospace'%3EKW%3C/text%3E%3C/svg%3E">
  <style is:inline>
    *{margin:0;padding:0;box-sizing:border-box}
    html{background:#030303}
    body{
      min-height:100vh;display:flex;align-items:center;justify-content:center;
      background:radial-gradient(900px 500px at 50% -10%,rgba(239,68,68,.08),transparent 70%),#030303;
      font-family:'Segoe UI',system-ui,sans-serif;color:#e4e4e7;padding:24px;
    }
    .box{text-align:center;max-width:560px}
    .kw{
      width:64px;height:64px;margin:0 auto 28px;border-radius:16px;display:flex;align-items:center;
      justify-content:center;font-weight:800;font-size:22px;color:#fff;
      background:linear-gradient(135deg,#ef4444,#dc2626);box-shadow:0 0 44px rgba(239,68,68,.35);
    }
    .code{
      font-family:'Bebas Neue','Segoe UI',sans-serif;font-size:120px;line-height:1;letter-spacing:6px;
      background:linear-gradient(135deg,#fca5a5,#ef4444);-webkit-background-clip:text;background-clip:text;color:transparent;
    }
    h1{font-size:20px;font-weight:600;margin:14px 0 10px;color:#fafafa}
    p{color:#9c8fa3;font-size:14.5px;line-height:1.7;margin-bottom:34px}
    .links{display:flex;gap:12px;justify-content:center;flex-wrap:wrap}
    a{
      display:inline-flex;align-items:center;gap:8px;padding:12px 22px;border-radius:99px;
      font-size:13.5px;font-weight:600;text-decoration:none;transition:transform .2s;
    }
    a:hover{transform:translateY(-2px)}
    .primary{background:linear-gradient(145deg,#ef4444,#dc2626);color:#fff;box-shadow:0 10px 26px -8px rgba(239,68,68,.5)}
    .ghost{border:1px solid rgba(255,255,255,.14);color:#d4d4d8}
    .ghost:hover{border-color:rgba(239,68,68,.4)}
  </style>
</head>
<body>
  <div class="box">
    <div class="kw">KW</div>
    <div class="code">404</div>
    <h1>Página não encontrada</h1>
    <p>A rota que você acessou não existe ou foi movida. Volte ao início do site ou navegue pela documentação do KT WIRZADE.</p>
    <div class="links">
      <a class="primary" href="/">← Voltar ao site</a>
      <a class="ghost" href="/docs">Documentação</a>
    </div>
  </div>
</body>
</html>
